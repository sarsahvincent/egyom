import React from "react";
import styles from "../styles/Home.module.css";

const SkillTag: React.FC<{}> = ({ children }) => {
  return <button style={{}} className={styles.skillTage}>{children}</button>;
};

export default SkillTag;
